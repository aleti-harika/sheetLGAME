function playGame(choice) {
            if (choice === 'C') {
                window.location.href = 'congrats.html';
            } else {
                window.location.href = 'lost.html';
            }
        }